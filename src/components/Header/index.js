import React from "react";
import "./style.css";

function Header() {
  return (
    <header className="header">
      <h2>Click on a Pokemon to catch it, but don't catch the same one twice!</h2>
    </header>
  );
}

export default Header;
